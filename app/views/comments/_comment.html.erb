
<p>
  <%= comment.body %>
  <small>
    <i> <%=comment.user.name %> commented <%= time_ago_in_words(comment.created_at) %> ago </i>
  </small>
  <%= link_to "Delete", [comment.discussion, comment], method: :delete, remote: true %>
</p>
