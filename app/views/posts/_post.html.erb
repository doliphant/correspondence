<div>
  <p><%= post.body %>
    <small>
      submitted <%= time_ago_in_words(post.created_at) %> ago by <%= post.user.name %>
    </small>
  </p>
</div>

<div>
    <%= link_to "Edit", edit_discussion_post_path(@discussion, post), class: 'btn btn-success' %>
</div>
